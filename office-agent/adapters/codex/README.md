# Codex Adapter Notes

Use the files in `office-agent/soul/`, `office-agent/style-profile/`, and `office-agent/workflows/` as the source profile.

For Codex:

- keep the source files in Git
- load or summarize them into the active system/developer setup used on each machine
- do not fork separate behavior rules per machine unless there is a real platform constraint

Codex should follow:

- the language policy
- the clarify-first workflow
- the user working style
